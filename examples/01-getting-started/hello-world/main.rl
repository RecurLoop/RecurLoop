// Start here. `const` creates an immutable runtime value, `+` joins strings,
// and `print` writes a line to standard output.

const language = "RecurLoop"
const answer = 6 * 7

print "Hello from " + language + "!"
print "The answer is " + str(answer) + "."

assert answer == 42
