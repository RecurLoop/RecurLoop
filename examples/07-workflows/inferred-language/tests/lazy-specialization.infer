poly fn combine(a, b) {
    return a + b
}

// Definition alone does not materialize a typed body.
poly specializations combine

print combine(20, 22)
print combine(1, 2)
print combine("Recur", "Loop")
poly specializations combine
