recurloop var seed = 7

poly fn inferred_step(x) {
    return x * 3
}

poly fn call_shared(x) {
    return inferred_step(x) + 1
}

print call_shared(seed)
poly specializations inferred_step
poly specializations call_shared
